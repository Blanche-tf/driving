<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd" >
<mapper namespace="driving.school.dao.ModulesDAO">
    <select id="findByParentCode" resultType="driving.entity.Modules">
        select * from jx_modules
        where parentCode = #{param1}
    </select>

</mapper>